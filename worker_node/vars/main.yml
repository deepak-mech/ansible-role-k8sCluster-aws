---
# vars file for worker_node
package_name:
        - "docker"
        - "iproute-tc"

k8s_package_name:
        - "kubelet"
        - "kubeadm"
        - "kubectl"

service_name:
        - "docker"
        - "kubelet"
